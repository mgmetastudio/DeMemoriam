import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const StorySettings = () => {
  return (
    <View>
      <Text>StorySettings</Text>
    </View>
  )
}

export default StorySettings

const styles = StyleSheet.create({})